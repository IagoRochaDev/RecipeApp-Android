package com.devrochaiago.recipeapp.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta Principal (Sabor & Energia)
val PrimaryOrange = Color(0xFFFF6D00) // Laranja "Cheetos" para os botões principais
val PrimaryYellow = Color(0xFFFFC107) // Amarelo mostarda
val PrimaryDark = Color(0xFFE65100)   // Laranja escuro

// Tons de Fundo e Superfície
val BackgroundLight = Color(0xFFFAFAFA) // Branco levemente gelo
val SurfaceLight = Color(0xFFFFFFFF)    // Branco puro pros cards
val BackgroundDark = Color(0xFF121212)  // Fundo do modo escuro
val SurfaceDark = Color(0xFF1E1E1E)     // Cards no modo escuro

// Textos
val TextDark = Color(0xFF212121)
val TextLight = Color(0xFFFFFFFF)
val TextGray = Color(0xFF757575)